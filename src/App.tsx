// App.tsx không còn được dùng — entry point đã chuyển sang main.tsx với RouterProvider.
// File này được giữ lại để tránh lỗi import cũ. Có thể xóa an toàn.
export default function App() {
  return null;
}
